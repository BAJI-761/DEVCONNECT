DevConnect client scaffold

Quick start:

1) Install dependencies

```powershell
cd client
npm install
```

2) Run dev server

```powershell
npm run dev
```

This is a minimal scaffold created by the implementation script. Continue building UI components under `src/components` and pages under `src/pages`.
